pipeline{
    agent any
    stages{
        stage ('Newman test'){
            steps{
                sh '''
                cd api/newman
                node --version
                npm install -g newman
                newman run TestsDeApi.postman_collection.json -e Heroku.postman_environment.json
                '''
            }

        }
    }



}